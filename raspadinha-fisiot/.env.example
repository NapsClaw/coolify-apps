# Preencher via variáveis de ambiente do Coolify (nunca commitar valores reais).
DATABASE_URL=postgresql://usuario:senha@host/banco?sslmode=require
JWT_SECRET=uma-string-aleatoria-longa-gerada-com-openssl-rand
